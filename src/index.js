import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// esto pega mi componente principal (App) en el index.html
ReactDOM.render(<App />, document.getElementById('root'));
